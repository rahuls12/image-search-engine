function ImageShow({ link }) {
    return <img src={link} style={{
            height:'200px',
            width: '200px',
            display: 'inline-flex'
        }} />
    
}

export default ImageShow;